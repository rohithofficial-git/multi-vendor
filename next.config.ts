import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['10.178.184.197', 'localhost', 'wide-emus-try.loca.lt', 'every-years-tell.loca.lt', 'localhost.run']
};

export default nextConfig;
